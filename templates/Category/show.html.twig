{% extends 'base.html.twig' %}
{% block title %}Wild Series{% endblock %}
{% block body %}
    {% include 'Wild/_header.html.twig' %}
    <div class="container w-100 p-3 bg-light">
        {% for serie in series %}
            <a href="{{ path("program_show", { id: serie.id }) }}">{{ serie.title }}</a>
            <p>{{ serie.synopsis }}</p>
            <p>{{ serie.id }}</p>
        {% endfor %}
    </div>
    <a href="{{ path('program_index') }}">
        Retour à la liste des programmes
    </a>
    </div>
{% endblock %}